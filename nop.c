#include "monty.h"
/**
 * _nop - does not do anything
 * @stack: stack pointer to head
 * @line: number of line of code
 */
void _nop(stack_t **stack, unsigned int line)
{
	(void)stack;
	(void)line;
}
